# barry-the-barcode-detector

To run, do the following:

```c++
make
./barry <example_name.png> <numthreads>
```

Example:

```c++
make
./barry example.png 4
```

(4 threads provides the best speedup)
